describe("Data Type", function () {
  it("should must declare", function () {
    const name: string = "Gilbert Hutapea";
    const balance: number = 1000000;
    const isVip: boolean = true;

    console.info(name);
    console.info(balance);
    console.info(isVip);

    // name = 100;
    // balance = "gilbert";
    // isVip = 100;
  });
});
